# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-001
workflow: story-delivery
current_stage: IMPACT_ANALYSIS
previous_stage: DESIGN_REVIEW
last_completed_stage: DESIGN_REVIEW
status: IN_PROGRESS
attempt: 1
last_invoked_skill: design-reviewer
last_result:
  verdict: PASS
  stage: DESIGN_REVIEW
  recorded_at: 2026-08-31T09:46:30Z
last_artifacts:
  - type: design_review
    path: docs/reviews/designs/US-001-design-review.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "DESIGN_REVIEW verdict PASS: API and DB designs sound, cross-consistent, compliant with the approved Specification and architecture/security conventions. No Critical/Major findings."
  - "D-1: password character-class policy is only in the OpenAPI description, not a pattern; TEST_WRITING must source password cases from Spec §6.2, not the schema."
  - "D-2 (confirmed): single Customer entity/table covers glossary Customer + Account for US-001 (no profile data); matches Spec NFR-4, PC-5, API naming. Future split not foreclosed (spec-review F-6 / API Q-1)."
  - "D-3 (confirmed): role modeled as an EnumType.STRING VARCHAR(20) column, not a customer_role table; PC-5 cites customer_role only as a naming example."
  - "D-4 (confirmed): ck_customer_role CHECK and role/enabled DEFAULT clauses are defensive DDL-only; acceptable to keep, not verified by ddl-auto=validate."
  - "D-5: OffsetDateTime <-> TIMESTAMP WITH TIME ZONE under ddl-auto=validate and H2 2.x IDENTITY clause ordering are known trip points; IMPLEMENTATION / IMPLEMENTATION_VERIFICATION own it."
  - "D-6: password maxLength 72 in the contract is characters; the real limit is 72 bytes (BCrypt input). TEST_WRITING / IMPLEMENTATION own the byte-boundary vector (spec-review F-5, API Q-4)."
  - "D-7: unknown-JSON-field rejection (additionalProperties: false -> 400) is the design-stage choice Spec §6.3 delegated to API_DESIGN; IMPLEMENTATION must wire Jackson FAIL_ON_UNKNOWN_PROPERTIES + advice mapping (API Q-3)."
  - "D-8: Location header targets GET /api/v1/customers/{id}, not implemented by US-001; header value is stable and correct."
  - "D-9: entity-model §6 mentions a 'customer' package; IMPACT_ANALYSIS / IMPLEMENTATION_PLANNING must use the package-map.md packages (model.entity, model.request, model.dto, ...), not a feature package (AD-8)."
  - "spec-review F-1 (still open, advisory): AC-007 annotation \"AC-2\" is ambiguous with Story AC-002; means api-conventions.md AC-2 (media type)."
  - "spec-review F-3 (still open, advisory): spec FR-2 bundles Content-Type (415) with the body-shape rule; the 'exactly email + password' rule is only in prose. Matters to TEST_WRITING AC mapping."
  - "DESIGN_REVIEW PASS recorded: design artifacts api_design, openapi, database_design, entity_model advanced DRAFT -> APPROVED (no human gate follows DESIGN_REVIEW)."
  - "HUMAN_SPEC_APPROVAL approved 2026-08-31T07:48:48Z by obutr <ayubutrym@gmail.com>. Open Decisions resolved: OD-001:A, OD-002:B, OD-003:A, OD-004:A, OD-005:A, OD-006:A. open_decisions.md v1 still shows entries as OPEN; approved resolutions are authoritative."
started_at: 2026-08-30T23:50:01Z
updated_at: 2026-08-31T09:46:30Z
completed_at: null
archived_at: null
