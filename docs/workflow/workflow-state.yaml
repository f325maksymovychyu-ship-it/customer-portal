# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-001
workflow: story-delivery
current_stage: SECURITY_REVIEW
previous_stage: IMPLEMENTATION_VERIFICATION
last_completed_stage: IMPLEMENTATION_VERIFICATION
status: IN_PROGRESS
attempt: 1
last_invoked_skill: implementation-verifier
last_result:
  verdict: PASS
  stage: IMPLEMENTATION_VERIFICATION
  recorded_at: 2026-08-31T12:25:03Z
last_artifacts:
  - type: implementation_verification
    path: docs/verification/US-001-implementation-verification.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "IMPL_VERIFICATION PASS: ./gradlew clean build independently reproduced — BUILD SUCCESSFUL (exit 0); 62 tests, 0 failures, 0 errors, 0 skipped across 7 suites. AC-001..AC-007 all VERIFIED with executable test evidence. 0 Critical, 0 Major, 5 Minor findings."
  - "MF-1: implementer corrected a malformed 61-char dummy BCrypt literal to 60 chars (2 occurrences) in test-writer-owned CustomerSchemaTest.caseInsensitiveDuplicateEmailCollidesOnTheUniqueConstraint (VARCHAR(60) per PC-9). git diff f6e7eae..fb833b0 confirms this is the ONLY change to a test-writer test; no assertion/scenario/outcome changed; pre-fix the test was a false RED (first INSERT errored before the constraint assertion). Was IMPLEMENTATION D-3. test-writer owns the suite; RECONCILIATION to note the ownership crossing."
  - "MF-2: implemented 400 (one combined missing/malformed/unknown-field message) and 415 error message strings differ verbatim from the OpenAPI illustrative examples; the 409 message matches exactly, AC-6 body shape preserved, tests assert status+shape only. Consistent with IMPLEMENTATION D-4 / plan-review F-4. Optional: openapi-designer aligns examples in a future revision."
  - "MF-3 (SECURITY_REVIEW / RECONCILIATION to weigh): CustomerService.register uses check-then-act (existsByEmail then save); a concurrent duplicate would trip uq_customer_email as DataIntegrityViolationException, which is unmapped and falls through to 500 rather than 409. No AC/test/design covers concurrency; anti-abuse out of scope (OD-005). Latent robustness gap, not a requirements defect; candidate for a follow-up Story."
  - "MF-4 (carried F-1): docs/decisions/US-001-open-decisions.md v1 still shows OD-001..OD-006 OPEN; authoritative resolutions (OD-001:A, OD-002:B, OD-003:A, OD-004:A, OD-005:A, OD-006:A) are in history.jsonl at HUMAN_SPEC_APPROVAL and applied correctly. Owned by us-clarifier (publish v2). No implementation impact."
  - "MF-5 (SECURITY_REVIEW to weigh): RegistrationRequest is a record, so its generated toString() includes the plaintext password component (Customer.toString() was hand-written to exclude passwordHash). No reachable leak path found — production code has NO logger / System.out / println (grep over src/main = no matches); GlobalExceptionHandler never touches the bound DTO. SEC-3 ('never logged') holds today; a defensive custom toString() is a SECURITY_REVIEW call."
  - "R-1 CLOSED (verified at IMPL_VERIFICATION): OffsetDateTime <-> TIMESTAMP WITH TIME ZONE validates under ddl-auto=validate on H2 2.x / Hibernate ORM 7.4.5; context boots clean on the test profile against the hand-written schema.sql."
  - "R-2 (SECURITY_REVIEW owns): security/SecurityConfig scopes BOTH the public matcher and the CSRF exemption to POST /api/v1/customers via one RequestMatcher; anyRequest().authenticated(); formLogin/httpBasic/logout disabled; HttpStatusEntryPoint(401). RegistrationSecurityPostureTest 4/4 green. 'CSRF still enforced elsewhere' stays unassertable in US-001 (no second writable endpoint)."
  - "R-3 CLOSED (verified): password 12..72 bound measured in UTF-8 BYTES in both PasswordPolicyValidator and the service re-check; PasswordPolicyValidatorTest + CustomerRegistrationApiTest cover 11/12/72/73-byte and the multi-byte 73-byte vector."
  - "R-6 CLOSED (verified): config/JpaAuditingConfig DateTimeProvider returns OffsetDateTime.now(ZoneOffset.UTC); CustomerPersistenceTest asserts the UTC offset on created_at and updated_at and created_at immutability."
  - "R-7 (SECURITY_REVIEW owns): @ValidPassword message is the fixed string 'Password does not meet the security policy.'; no-echo asserted by PasswordPolicyValidatorTest.defaultMessageNeverEchoesASubmittedValue and CustomerRegistrationApiTest.passwordValidationMessageDoesNotEchoTheSubmittedValue."
  - "R-8 (RECONCILIATION owns): scope held — no GET /api/v1/customers/{id}, no login/UserDetailsService, no rate limiting. Change set = 16 new production files + InvalidPasswordException + 3 test files + M-1..M-4; the only change outside that is the benign CustomerSchemaTest fixture fix (MF-1). git confirms no unrelated changes."
  - "D-1 accepted at IMPL_VERIFICATION: exception/InvalidPasswordException is a justified required supporting change (FR-6 service re-check; plan C-12 left the type unspecified); domain exception in the exception package (AD-6), mapped to 400, not reachable from the HTTP flow."
  - "D-2 accepted at IMPL_VERIFICATION: CustomerPersistenceTest uses @SpringBootTest instead of the indicative @DataJpaTest skeleton; all three C-T2 scenarios covered and passing."
  - "D-5 (carried): schema.sql id column uses 'GENERATED BY DEFAULT AS IDENTITY' with no explicit NOT NULL (H2 2.x implicit); ddl-auto=validate passes on both profiles; CustomerSchemaTest.idColumnIsBigintNotNull confirms BIGINT + not-nullable."
  - "Test-isolation smell (not a finding): CustomerSchemaTest and CustomerPersistenceTest share one JVM-lifetime in-memory H2 (DB_CLOSE_DELAY=-1); CustomerSchemaTest inserts two non-transactional rows that outlive the test. Harmless because all other tests use randomized emails."
  - "ORCHESTRATOR STATE REPAIR (TEST_WRITING continue, 2026-08-31): docs/plans/US-001-implementation-plan.md front-matter status was still DRAFT after PLAN_REVIEW (PASS) and HUMAN_PLAN_APPROVAL (2026-08-31T11:20:00Z). Corrected to status: APPROVED, updated_at 2026-08-31T11:20:00Z. No version bump, no content change. (Retained for audit.)"
started_at: 2026-08-30T23:50:01Z
updated_at: 2026-08-31T12:25:03Z
completed_at: null
archived_at: null
