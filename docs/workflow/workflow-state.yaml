# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-001
workflow: story-delivery
current_stage: IMPLEMENTATION_VERIFICATION
previous_stage: IMPLEMENTATION
last_completed_stage: IMPLEMENTATION
status: IN_PROGRESS
attempt: 1
last_invoked_skill: springboot-implementor
last_result:
  verdict: PASS
  stage: IMPLEMENTATION
  recorded_at: 2026-08-31T12:09:46Z
last_artifacts:
  - type: implementation_report
    path: docs/evidence/US-001-implementation-report.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "IMPLEMENTATION D-1: added exception/InvalidPasswordException for the FR-6 service-layer password re-check (plan C-12 left the type unspecified). Domain exception, mapped to 400 by the advice, not reachable from the HTTP flow (request @Valid catches policy failures first). No requirement/design change; no loop-back."
  - "IMPLEMENTATION D-2: persistence/CustomerPersistenceTest (plan skeleton C-T2) uses @SpringBootTest instead of the indicative @DataJpaTest; all three deferred C-T2 scenarios (UTC auditing, created_at immutability, EnumType.STRING round-trip) are covered and pass."
  - "IMPLEMENTATION D-3 (implementer-modified test — IMPLEMENTATION_VERIFICATION please confirm): src/test/java/.../persistence/CustomerSchemaTest.java was edited — a malformed 61-character dummy BCrypt literal was shortened to 60 chars (…wxyz012345 -> …wxyz01234, 2 occurrences) in caseInsensitiveDuplicateEmailCollidesOnTheUniqueConstraint so the fixture INSERT fits password_hash VARCHAR(60) (PC-9 / DB design Q-2, asserted by the same file's passwordHashColumnIsVarchar60NotNull). No assertion, scenario, or expected outcome changed. Case-insensitive duplicate rejection has independent coverage in CustomerRegistrationApiTest.duplicateEmailIsRejectedCaseInsensitivelyWith409 and CustomerServiceTest. test-writer owns the suite and may re-issue the fixture."
  - "IMPLEMENTATION D-4 / plan-review F-4: malformed JSON and unknown-field share one HttpMessageNotReadableException advice branch and one generic 400 message (PD-1); OpenAPI's two example messages are illustrative, AC-6 body shape preserved, tests assert status+shape only."
  - "IMPLEMENTATION D-5: schema.sql id column uses 'GENERATED BY DEFAULT AS IDENTITY' with no explicit NOT NULL (H2 2.x implicit; adding the clause is rejected on this version). ddl-auto=validate passes on both profiles; CustomerSchemaTest.idColumnIsBigintNotNull confirms BIGINT + not-nullable. spring.sql.init.mode=always on file H2 re-runs schema.sql each boot; CREATE TABLE IF NOT EXISTS makes it idempotent."
  - "R-1 CLOSED at implementation: ./gradlew clean build green (62 tests, 0 fail); ddl-auto=validate boots clean on the test (in-memory) and default (file H2) profiles; OffsetDateTime <-> TIMESTAMP WITH TIME ZONE validates on H2 2.x / Hibernate ORM 7.4.5."
  - "R-2 (SECURITY_REVIEW owns): security/SecurityConfig scopes BOTH the public matcher and the CSRF exemption to POST /api/v1/customers via one RequestMatcher; anyRequest().authenticated(); formLogin/httpBasic/logout disabled; HttpStatusEntryPoint(401). 'CSRF still enforced elsewhere' stays unassertable in US-001 (no second writable endpoint) — RegistrationSecurityPostureTest covers what can be asserted."
  - "R-3 CLOSED at implementation: password 12..72 bound measured in UTF-8 BYTES in both PasswordPolicyValidator.isCompliant and the service re-check; PasswordPolicyValidatorTest covers 11/12/72/73-byte and multi-byte (\"Aa1!\"+\"€\"×23 = 73 bytes) vectors, plus the HTTP-level passwordOver72BytesButUnder72CharsReturns400."
  - "R-6 CLOSED: config/JpaAuditingConfig DateTimeProvider returns OffsetDateTime.now(ZoneOffset.UTC); CustomerPersistenceTest.auditingPopulatesBothTimestampsInUtc asserts the UTC offset on created_at and updated_at."
  - "R-7 (SECURITY_REVIEW owns): @ValidPassword message is a fixed generic string ('Password does not meet the security policy.'); PasswordPolicyValidatorTest.defaultMessageNeverEchoesASubmittedValue and CustomerRegistrationApiTest.passwordValidationMessageDoesNotEchoTheSubmittedValue assert no value echo. Error bodies also checked by errorBodyNeverLeaksInternals."
  - "R-5 CLOSED: M-4 added @ActiveProfiles(\"test\") to CustomerPortalApplicationTests; contextLoads passes with Security + JPA active."
  - "R-8 (RECONCILIATION owns): scope held — no GET /api/v1/customers/{id}, no login/UserDetailsService, no rate limiting. Change set is 16 new production files + InvalidPasswordException + 3 test files + M-1..M-4; no file changed outside plan scope except the CustomerSchemaTest fixture fix (D-3)."
  - "F-1 (carried): docs/decisions/US-001-open-decisions.md v1 still shows OD-001..OD-006 as OPEN; authoritative resolutions (OD-001:A, OD-002:B, OD-003:A, OD-004:A, OD-005:A, OD-006:A) recorded in history.jsonl at HUMAN_SPEC_APPROVAL and applied throughout. us-clarifier should publish v2. Non-blocking, no loop-back."
  - "F-a (carried from TEST_WRITING): deferred unit depth is now implemented — PasswordPolicyValidatorTest (C-T1, 17 tests), CustomerServiceTest (C-T3, 5 tests), CustomerPersistenceTest (C-T2, 3 tests). Full suite: 62 tests, 0 failures (baseline red phase was 37 tests / 33 failed)."
  - "F-b (carried from TEST_WRITING, plan F-3 / R-2): RegistrationSecurityPostureTest.protectedRouteReturns401WhenUnauthenticated passes — IMPLEMENTATION adopted HttpStatusEntryPoint(401) (the plan's recommendation), not form login, so the tripwire is green. SECURITY_REVIEW still arbitrates final CSRF-scope posture."
  - "F-c (carried from TEST_WRITING): build.gradle.kts now DOES add spring-boot-starter-validation (plan M-1, was TEST_WRITING-deferred). src/test/resources/application-test.yaml was NOT modified (test-writer's C-17); IMPLEMENTATION relied on it as-is."
  - "ORCHESTRATOR STATE REPAIR (TEST_WRITING continue, 2026-08-31): docs/plans/US-001-implementation-plan.md front-matter status was still DRAFT after PLAN_REVIEW (PASS) and HUMAN_PLAN_APPROVAL (2026-08-31T11:20:00Z). Corrected to status: APPROVED, updated_at 2026-08-31T11:20:00Z. No version bump, no content change. (Retained for audit.)"
  - "F-2 (carried): plan execution-order per-step completion criteria for steps 6-10 reference assertions that only pass after steps 10-11; step 13 (./gradlew clean build) is the authoritative gate — now green."
  - "F-4 (carried): PD-1 maps malformed JSON and unknown-field both through one HttpMessageNotReadableException branch (one message); OpenAPI shows two distinct example messages. Examples illustrative; AC-6 shape preserved. See D-4."
started_at: 2026-08-30T23:50:01Z
updated_at: 2026-08-31T12:09:46Z
completed_at: null
archived_at: null
